node_modules/
dist/
.astro/
.env
.env.local

# Generated by scripts/build_manifest.py — fresh on every npm run build/dev.
src/data/cases/
src/data/index.json

# Legacy local walker output before the auto_messages/ rename (see
# generate_messages.py DEFAULT_OUT_DIR). Kept ignored so a stale local
# checkout doesn't show it as untracked.
src/data/messages/

# Per-case packet captures live on the orphan `pcap-data` branch and are
# overlaid onto site/src/data/pcap/ during CI build (see site.yml). Local
# `npm run dev` works without them — the pcap section is skipped gracefully.
src/data/pcap/
