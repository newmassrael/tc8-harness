{
  "description": "Verify that SubscribeEventgroup with Entries Array length cut — DUT ignores (or Nacks defensively)",
  "approach": "[Objective]\nEntries array length declares 0 (no entries) — DUT must not respond as if entries existed.\n\n[Scenario — chronological]\n1. DUT → Tester: OfferService with IPv4 endpoint option.\n2. Tester → DUT: SubscribeEventgroup (per-case shape).\n3. Observation: DUT → Tester: SubscribeAck/Nack/event per the asserted behaviour.\n\n[Pass condition]\nObserved DUT response matches the spec property.\n\n[Fail condition]\nPer-phase Fail_phase<n>_* per the verdict list.",
  "verdicts": {
    "Pass": "DUT rejected/ignored malformed subscribe — normal",
    "Fail_phase1_no_offer": "No OfferService within listen window",
    "Fail_phase2_dut_responded": "DUT responded to zero entries"
  }
}
