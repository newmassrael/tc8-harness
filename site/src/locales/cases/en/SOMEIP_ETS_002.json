{
  "description": "Verify that Length field overshoots actual payload — DUT must reject or ignore",
  "approach": "[Objective]\nInner array length overshoots — same shape as _001 but different field; DUT must reject.\n\n[Scenario — chronological]\n1. DUT → Tester: OfferService.\n2. Tester → DUT: Method Request (per-case shape).\n3. Observation: DUT → Tester: Method Response (or absence) per the asserted behaviour.\n\n[Pass condition]\nDUT-emitted response satisfies the spec property.\n\n[Fail condition]\nPer-phase Fail_phase<n>_* per the verdict list.",
  "verdicts": {
    "Pass": "DUT rejected/ignored malformed input — normal",
    "Fail_phase1_no_offer": "No OfferService within listen window",
    "Fail_phase2_dut_accepted_malformed": "DUT returned OK Response for array length too long"
  }
}
