{
  "description": "Verify that UTF16FIXED 65-byte payload — DUT must respond (any return_code)",
  "approach": "[Objective]\nTolerant: 65 bytes is 1 over the fixed slot — DUT may canonicalise (echo 64) or reject; either way it must respond, not silent-drop.\n\n[Scenario — chronological]\n1. DUT → Tester: OfferService.\n2. Tester → DUT: UTF16FIXED method with 65-byte payload.\n3. Observation: DUT → Tester: any Method Response.\n\n[Pass condition]\nObserved DUT behaviour matches the spec property.\n\n[Fail condition]\nPer-phase Fail_phase<n>_* per the verdict list.",
  "verdicts": {
    "Pass": "DUT responded (any code) — normal",
    "Fail_phase1_no_offer": "No OfferService within listen window",
    "Fail_phase2_no_response": "No Method Response within listen window for oversized"
  }
}
