{
  "case_id": "SOMEIP_ETS_091",
  "generator_version": "2",
  "messages": [
    {
      "idx": 0,
      "role": "expected",
      "label": "Phase 1 (listening_phase1) progress trigger — advances to listening_phase2. Cond: service_id == 0xFFFF and sd_entry_count >= 1 and sd_entries[0].type == 0x01 and sd_entries[0].service_id == expected.service_id and sd_ipv4…"
    },
    {
      "idx": 1,
      "role": "expected",
      "label": "Phase 2 (listening_phase2) pass trigger (candidate — opaque cond clause UNKNOWN). Cond: service_id == 0xFFFF and sd_entry_count >= 1 and sd_entries[0].type == 0x01 and sd_entries[0].service_id == expected.service_id and session…"
    },
    {
      "idx": 2,
      "role": "expected",
      "label": "Phase 2 (listening_phase2) pass trigger (candidate — opaque cond clause UNKNOWN). Cond: service_id == 0xFFFF and sd_entry_count >= 1 and sd_entries[0].type == 0x01 and sd_entries[0].service_id == expected.service_id and session…"
    },
    {
      "idx": 3,
      "role": "note",
      "label": "Phase 2 (listening_phase2) — DUT frame (no transition matched)"
    },
    {
      "idx": 4,
      "role": "expected",
      "label": "Phase 2 (listening_phase2) pass trigger (candidate — opaque cond clause UNKNOWN). Cond: service_id == 0xFFFF and sd_entry_count >= 1 and sd_entries[0].type == 0x01 and sd_entries[0].service_id == expected.service_id and session…"
    },
    {
      "idx": 5,
      "role": "expected",
      "label": "Phase 2 (listening_phase2) pass trigger (candidate — opaque cond clause UNKNOWN). Cond: service_id == 0xFFFF and sd_entry_count >= 1 and sd_entries[0].type == 0x01 and sd_entries[0].service_id == expected.service_id and session…"
    }
  ]
}