{# SPDX-License-Identifier: LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial #}
{# SPDX-FileCopyrightText: Copyright (c) 2025 newmassrael #}

{%- import '_macros/sce_map_marker.jinja2' as sce_map %}
{{ sce_map.source_marker(source_location, "cpp", module_level=true) }}

// SCE Forge: Auto-generated from Extended SCXML (sce:kind="lookup")
// Runtime: {{ runtime_dep }}
// Do not edit — regenerate from the source SCXML file.

#pragma once
#ifndef {{ guard }}
#define {{ guard }}

#include <cstdint>
{% if not output_is_string %}
#include <optional>
#include "sce/forge/lookup.h"
{% endif %}
{% if all_imports %}
{% for imp in all_imports %}
{{ imp.include_stmt }}
{% endfor %}
{% endif %}

namespace SCE::Generated::{{ namespace }} {
{% if output_is_string %}

enum class {{ enum_name }} { {{ unique_values | join(", ") }} };

inline {{ enum_name }} {{ func_name }}({{ input_type }} {{ input_id }}) {
    switch ({{ input_id }}) {
{% for group in entries_by_value %}
{% for key in group.keys %}
        case {{ key }}:
{% endfor %}
            return {{ enum_name }}::{{ group.value }};
{% endfor %}
        default: return {{ enum_name }}::{{ default_value }};
    }
}
{% else %}

constexpr {{ input_type }} KEYS[{{ n }}] = { {{ keys_literal | join(", ") }} };
constexpr {{ value_type }} VALUES[{{ n }}] = { {{ values_literal | join(", ") }} };

{% if on_miss_error %}
inline std::optional<{{ value_type }}> {{ func_name }}({{ input_type }} {{ input_id }}) {
    return SCE::Forge::lookup(KEYS, VALUES, {{ input_id }});
}
{% else %}
inline {{ value_type }} {{ func_name }}({{ input_type }} {{ input_id }}) {
    return SCE::Forge::lookup(KEYS, VALUES, {{ input_id }}).value_or({{ default_literal }});
}
{% endif %}
{% endif %}

}  // namespace SCE::Generated::{{ namespace }}

#endif  // {{ guard }}
