{# SPDX-License-Identifier: LGPL-2.1-or-later WITH LicenseRef-SCE-Linking-Exception OR LicenseRef-SCE-Commercial #}
{# SPDX-FileCopyrightText: Copyright (c) 2025 newmassrael #}

{%- import '_macros/sce_map_marker.jinja2' as sce_map %}
{{ sce_map.source_marker(source_location, "kotlin", module_level=true) }}

// SCE Forge: Auto-generated from Extended SCXML (sce:kind="filter")
// Runtime: {{ runtime_dep }}
// Do not edit — regenerate from the source SCXML file.

package com.sce.generated.{{ package }}

{% if filter_type == "moving_average" %}
import com.sce.forge.runtime.MovingAverage
{% elif filter_type == "low_pass" %}
import com.sce.forge.runtime.LowPass
{% elif filter_type == "debounce" %}
import com.sce.forge.runtime.Debounce
{% endif %}

class {{ struct_name }} {
{% if filter_type == "moving_average" %}
    private val impl = MovingAverage(window = {{ window }})
{% elif filter_type == "low_pass" %}
    private val impl = LowPass(alpha = {{ alpha }})
{% elif filter_type == "debounce" %}
    private val impl = Debounce<{{ output_type }}>(window = {{ window }})
{% endif %}

    fun update({{ input_id }}: {{ input_type }}): {{ output_type }} {
{% if input_type == output_type %}
        return impl.update({{ input_id }})
{% else %}
        return impl.update({{ input_id }}.to{{ output_type }}())
{% endif %}
    }

    fun reset() {
        impl.reset()
    }
}
