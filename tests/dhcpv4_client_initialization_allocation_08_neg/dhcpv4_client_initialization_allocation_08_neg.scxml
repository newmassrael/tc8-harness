<?xml version="1.0" encoding="UTF-8"?>
<!--
  TC8 §4.7.6.9 DHCPv4_CLIENT_INITIALIZATION_ALLOCATION_08 negative —
  closes the self-reference trap on the positive _08 case (the post-BOUND
  ARP Probe MUST carry sender_proto_ip = 0, RFC 2131 §4.4.1 / RFC 5227
  §2.1.1). The positive's fail_arp_probe_shape_mismatch branch is
  unreachable against a conformant tc8-dut, which emits sender_ip = 0 by
  construction.

  Fault injection is a real firmware mutant, not a harness-composed frame:
  the stimulus drives the DHCP client with the kDhcpFlavorProbeSenderIpNonzero
  flavor (arp_probe_listen_ms = 1500 so the full lifecycle reaches the
  post-BOUND Probe). tc8-dut's emitArpProbe writes a non-zero sentinel
  sender IP (192.0.2.8). A conformant client (flavor None) probes with
  sender_ip = 0, so this negative's fail_compliant branch is the live
  conformant-DUT outcome and a flavor-wiring regression catch.

  Pass = the malformed Probe IS observed (opcode 1, non-zero sender — so
  is_dhcp_arp_probe is false). fail_compliant = the client emitted a valid
  Probe despite the flavor.
-->
<scxml xmlns="http://www.w3.org/2005/07/scxml"
       xmlns:sce="http://sce.dev/ext"
       xmlns:cpp="urn:sce:cpp"
       version="1.0"
       name="dhcpv4_client_initialization_allocation_08_neg"
       datamodel="null"
       initial="listening_for_discover">

  <sce:context id="captured"
               cpp:type="tc8::ArpAndDhcpv4Captured"
               cpp:include="sce_integration/arp_and_dhcpv4_captured.h"/>

  <sce:context id="expected"
               cpp:type="tc8::ArpAndDhcpv4Expected"
               cpp:include="sce_integration/arp_and_dhcpv4_expected.h"/>

  <state id="listening_for_discover">
    <onentry>
      <send event="s1_deadline" delay="6s"/>
    </onentry>
    <transition event="dhcp_observed"
                cond="cpp:captured.dhcpv4.is_dhcp_discover()
                      and captured.dhcpv4.chaddr_matches_dut_mac(expected.dhcpv4.dut_iface_mac)"
                target="listening_for_first_request"/>
    <transition event="s1_deadline" target="inconclusive_no_discover"/>
  </state>

  <state id="listening_for_first_request">
    <onentry>
      <send event="s2_deadline" delay="4s"/>
    </onentry>
    <transition event="dhcp_observed"
                cond="cpp:captured.dhcpv4.is_dhcp_request()
                      and captured.dhcpv4.chaddr_matches_dut_mac(expected.dhcpv4.dut_iface_mac)"
                target="listening_for_arp_probe"/>
    <transition event="s2_deadline" target="inconclusive_no_request"/>
  </state>

  <state id="listening_for_arp_probe">
    <onentry>
      <send event="s3_deadline" delay="8s"/>
    </onentry>
    <transition event="arp_observed"
                cond="cpp:captured.arp.opcode == 1
                      and !captured.arp.is_dhcp_arp_probe(expected.dhcpv4.offered_ip_be)"
                target="pass"/>
    <transition event="arp_observed"
                cond="cpp:captured.arp.is_dhcp_arp_probe(expected.dhcpv4.offered_ip_be)"
                target="fail_compliant"/>
    <transition event="s3_deadline" target="inconclusive_no_arp_probe"/>
  </state>

  <final id="pass">
    <donedata>
      <content>{"verdict":"pass"}</content>
    </donedata>
  </final>

  <final id="fail_compliant">
    <donedata>
      <content>{"verdict":"fail","reason":"dut_arp_probe_carried_zero_sender_ip_despite_nonzero_flavor","role":"observed_violation"}</content>
    </donedata>
  </final>

  <final id="inconclusive_no_discover">
    <donedata>
      <content>{"verdict":"inconclusive","reason":"no_dut_dhcp_discover_within_listen_window","role":"precondition_unmet"}</content>
    </donedata>
  </final>

  <final id="inconclusive_no_request">
    <donedata>
      <content>{"verdict":"inconclusive","reason":"no_dut_dhcp_request_after_offer","role":"property_unobserved"}</content>
    </donedata>
  </final>

  <final id="inconclusive_no_arp_probe">
    <donedata>
      <content>{"verdict":"inconclusive","reason":"no_dut_arp_probe_after_bound","role":"property_unobserved"}</content>
    </donedata>
  </final>
</scxml>
