{
  "doc": "Deferred negatives (debt D7 -- docs/verdict_policy.md Section 6). Positive cases whose verdict guard IS expect-flippable (it compares against an operator-supplied expected value) but which carry no sound NEG_ROW yet, each with an explicit reason. Unlike the conformant-absence registry (DUT-behaviour guards that no --expect can fault), these can and should eventually get a sound expect-flip negative or a parameterisation; the reason records why they don't yet (Linux deviation makes the positive itself fail, the guard checks a compiled constant that must be lifted to applyExpectedDefaults, etc.). Tracked here so the exhaustiveness audit accounts for them rather than letting them fall through. Prefer fixing over deferring.",
  "cases": {
    "IPv4_REASSEMBLY_11": "needs-sound-negative: Linux ipfrag_time deviation makes the positive itself land on fail_timeout (no Echo Reply), so an echo_id-flip negative carries zero diagnostic variance",
    "IPv4_REASSEMBLY_13": "needs-sound-negative: overlapping-fragment reassembly is dropped post-CVE-2018-5391 on Linux, so the positive does not reassemble; deferred with REASSEMBLY_11",
    "ICMPv4_TYPE_08": "needs-parameterise: echo payload is checked against a compiled constant (kIcmpv4EchoPayloadType08); lift to applyExpectedDefaults + expected.payload like the ETS echoes, then add a sound payload-flip row",
    "ICMPv4_TYPE_09": "needs-verification: echo_id-flip negative is structurally sound (twin of the CI-verified TYPE_12) but its own row is unverified on the wire; promote to a SOUND_ROW after a smoke run shows it lands on fail:echo_id_mismatch (not skip)",
    "DHCPv4_CLIENT_ALLOCATING_10": "template-artifact: a count-only liveness case (2 RENEWING REQUESTs) whose sole fail final (unreachable_pass_expr_always_true) is dead under the dhcpv4_renewing_retx_field binding pass_expr=true (fail cond is !(pass_expr) = false); no reachable fail to validate. Fix is to give the template a no-fail liveness variant, then re-register as a liveness guard",
    "SOMEIPSRV_FORMAT_23": "expect-flippable: SubscribeAck entry service_id checked vs --expect service_id (gate is entry type 0x07 literal); add a service_id-flip sound row and verify on CI smoke (precedent SOMEIPSRV_SD_MESSAGE_11)",
    "SOMEIPSRV_FORMAT_24": "expect-flippable: SubscribeAck entry instance_id checked vs --expect instance_id (gate is entry type 0x07 literal); add an instance_id-flip sound row and verify on CI smoke (precedent SOMEIPSRV_FORMAT_16)",
    "SOMEIPSRV_FORMAT_25": "expect-flippable: SubscribeAck entry major_version checked vs --expect major_version (gate is entry type 0x07 literal); add a major_version-flip sound row and verify on CI smoke (precedent SOMEIPSRV_SD_MESSAGE_03)",
    "SOMEIPSRV_FORMAT_26": "expect-flippable: SubscribeAck entry ttl checked vs --expect ttl (gate is entry type 0x07 literal); add a ttl-flip sound row and verify on CI smoke (precedent SOMEIPSRV_SD_MESSAGE_07)",
    "SOMEIPSRV_FORMAT_28": "expect-flippable: SubscribeAck entry eventgroup_id checked vs --expect eventgroup_id (gate is entry type 0x07 literal); add an eventgroup_id-flip sound row and verify on CI smoke",
    "SOMEIPSRV_ONWIRE_01": "expect-flippable: response src endpoint checked vs --expect dut_iface_ip/udp_port (gate is service_id + message_type 0x80); add a dut_iface_ip or udp_port flip sound row and verify on CI smoke (precedent SOMEIPSRV_OPTIONS_04/07)",
    "SOMEIPSRV_ONWIRE_06": "expect-flippable: response interface_version checked vs --expect major_version (gate is service_id + message_type 0x80); add a major_version-flip sound row and verify on CI smoke (precedent SOMEIPSRV_SD_MESSAGE_03)",
    "SOMEIPSRV_OPTIONS_14": "expect-flippable: IPv4 Multicast Option port checked vs --expect mcast_port (gate is entry type 0x07 + multicast count); add a mcast_port-flip sound row and verify on CI smoke (precedent SOMEIPSRV_OPTIONS_11)",
    "SOMEIPSRV_RPC_18": "expect-flippable: error response service_id echo checked vs --expect service_id (gate is message_type 0x81); add a service_id-flip sound row and verify on CI smoke",
    "SOMEIPSRV_RPC_20": "expect-flippable: error response interface_version checked vs --expect major_version (gate is message_type 0x81 + service_id); add a major_version-flip sound row and verify on CI smoke",
    "SOMEIPSRV_SD_MESSAGE_04": "expect-flippable: OfferService entry major_version checked vs --expect major_version; add a major_version-flip sound row and verify on CI smoke (precedent SOMEIPSRV_SD_MESSAGE_03)",
    "SOMEIPSRV_SD_MESSAGE_06": "expect-flippable: OfferService entry minor_version checked vs --expect minor_version; add a minor_version-flip sound row and verify on CI smoke (precedent SOMEIPSRV_SD_MESSAGE_05)",
    "SOMEIP_ETS_021": "needs-parameterise: int8 echo response payload is checked against a compiled constant (0xD6); lift to applyExpectedDefaults + expected.payload like the other ETS echoes (precedent SOMEIP_ETS_027) and add a sound payload-flip row"
  }
}
